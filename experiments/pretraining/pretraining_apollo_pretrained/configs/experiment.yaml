# Base config file for experiment settings
# Path: crbr/config_templates/experiment.yaml

project_name: CMB/pretraining                  # Top-level ClearML organizational/dir name (can also be set with --project-name)
task_name: pretrain_apollo_pretrained                     # Optional name for the run (usually set from cmd with -t / --task-name)
task_description: Pretraining of Pretrained-Apollo        # Optional text description of the experiment (can also be set with --task-description)
