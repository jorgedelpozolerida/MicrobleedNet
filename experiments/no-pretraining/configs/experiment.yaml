# Base config file for experiment settings
# Path: crbr/config_templates/experiment.yaml

project_name: CMB/no_pretraining                  # Top-level ClearML organizational/dir name (can also be set with --project-name)
task_name: train_apollo_pretrained                 # Optional name for the run (usually set from cmd with -t / --task-name)
task_description: Train pre-trained Apollo directly on real CMB         # Optional text description of the experiment (can also be set with --task-description)
